const CustomPlayIcon = () => {
  const iconStye = {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    width: "70px",
    height: "70px",
    backgroundColor: "yellow",
    borderRadius: "50%",
  };

  return <div style={iconStye}>Play</div>;
};

export default CustomPlayIcon;
